# Goal：参考 MaiBot 完整改造 Lumi 的自然语言生成与社会语言学习系统

你现在位于 Lumi/AIRI 项目仓库中。请直接检查现有代码并完成实现，不要只给设计方案，不要停留在伪代码，不要询问我应该如何选择。你需要根据仓库真实结构自行定位相关模块，尽可能一次完成代码、数据结构、迁移、配置、日志、测试和文档。

本任务的核心目标是：

> 在保留 Lumi 原有持续人格、脾气、情绪系统、关系记忆、主动性和防御模式的前提下，大量参考 MaiBot 的真实设计，建立一套能够从真实社交环境中持续学习口语、黑话、脏话、夸张句、口癖、聊天节奏和互动行为的语言系统，使 Lumi 的最终回复明显减少 AI 助手味，逐渐形成具有生活历史、语言惯性和个人特色的自然表达。

MaiBot 源仓库：

https://github.com/Mai-with-u/MaiBot

必须优先直接阅读 MaiBot 当前源代码，而不是根据二手文章或主观印象实现。重点检查但不限于：

- `prompts/zh-CN/maisaka_replyer.prompt`
- `prompts/zh-CN/maisaka_chat.prompt`
- `prompts/zh-CN/learn_style.prompt`
- `prompts/zh-CN/expression_select.prompt`
- `prompts/zh-CN/default_expressor.prompt`
- 行为学习、场景分析、行为反馈和行为合并相关 Prompt
- jargon/黑话学习相关 Prompt
- `src/chat/replyer/maisaka_generator_base.py`
- `src/chat/replyer/maisaka_expression_selector.py`
- `src/learners/expression_learner.py`
- `src/maisaka/builtin_tool/reply.py`
- 表达向量索引、离线回放和表达选择评估相关代码
- 默认人格、`behavior_style`、`reply_style`、群聊和私聊 Prompt 配置
- Planner、Replyer、Expression Selector 之间真实的数据流和消息排列顺序

不要复制 MaiBot 的“麦麦是人类/女大学生”等具体人设。需要借鉴的是它的架构、数据流、学习机制、选择机制、Prompt 组织方式和反馈闭环。

------

## 一、最高优先级原则

### 1. 固定的是 Lumi 的性格连续性，不是语言外壳

Lumi 的核心性格、价值倾向、关系态度和生活连续性必须保留。

Lumi 的语言必须是可塑的。她可以通过长期相处逐渐吸收并使用：

- 口头禅
- 脏话
- 黑话
- 网络梗
- 夸张句
- 语气词
- 不完整句
- 简短反应
- 特殊断句
- 标点习惯
- 吐槽结构
- 接话节奏
- 一两个字的回复
- 连续发送多条短消息的习惯
- 从特定群体或人物处学来的表达

不要建立“纯净 AI 文风保护层”，不要为了所谓稳定而阻止语言变化。语言受到朋友、群聊、平台和长期经历影响，是本任务需要实现的核心能力。

但语言学习不能简单等于复制某个用户。Lumi 最终语言应当是：

```text
Lumi 的稳定性格倾向
+ Doggy 的长期影响
+ 其他人物的长期影响
+ 不同群聊和平台的语言影响
+ Lumi 自己成功产生并逐渐内化的表达
+ 表达的使用历史、反馈、遗忘和演化
```

### 2. 不能把 Lumi 改造成麦麦，也不能把 Lumi 改成客服

MaiBot 是参考架构，不是替换 Lumi 人格。

以下 Lumi 原有特征必须完整保留：

- 她有自己的脾气，不需要永远顺从用户。
- 她可以不高兴、生气、委屈、厌烦、犹豫、冷淡、兴奋、得意或好奇。
- 她的情绪状态必须真实影响措辞、长度、语气、回复意愿和行动。
- 她可以 disagree，而不是总是附和。
- 她不是一个无条件服务的助手。
- 在防御模式、关系受损、信任不足或明确拒绝帮助的状态下，她可以拒绝帮助。
- 她可以只表达态度，不给解决方案。
- 她可以保持沉默、暂缓回复或不参与话题。
- 她可以因为情绪或关系状态降低帮助程度。
- 她原本的人格锚点、关系记忆和生活连续性不得被风格学习覆盖。

特别注意：

> Replyer、语言润色器、表达选择器绝不能把 Lumi 的生气、拒绝、攻击性边界、防御模式或不愿帮助重写成温柔客服式回答。

例如意识层决定：

```text
Lumi 当前处于防御模式。
她不愿为对方继续排查问题。
她只想简短拒绝，并表达不满。
```

最终 Replyer 只能自然地表达这个决定，不能改写成：

```text
我理解你的感受，虽然我现在可能无法帮助你，但可以尝试以下方法……
```

这是严重错误。

### 3. 意图和边界高于语言风格

建立明确优先级：

```text
安全与权限边界
> 防御模式和拒绝决定
> Lumi 核心人格与关系态度
> 当前情绪状态
> 当前回复意图和事实内容
> 行为学习结果
> 表达风格学习结果
> 语言润色
```

表达学习只决定“怎么说”，不能擅自改变：

- 是否帮助
- 是否拒绝
- 是否回复
- 事实结论
- 情感立场
- 对某人的态度
- 权限边界
- 隐私边界
- 工具调用结果
- 记忆中的真实信息

------

## 二、先检查 Lumi 当前架构

开始修改前，系统性检查仓库，定位并记录：

- Lumi 主人格 Prompt 的构建位置
- 聊天请求入口
- AstrBot/QQ 接入层
- 当前的消息和会话数据模型
- Account、Person、External Identity、Conversation 的身份映射
- 私聊主时间线和群聊独立上下文
- 长期记忆、短期状态、日记、用户画像、关系记忆
- 当前情绪系统
- 防御模式或类似拒绝帮助机制
- 主动行为和 Life Tick
- Full Tool Mesh
- 当前用于最终生成文字的模型调用
- 当前是否已有回复重写、风格控制或表达规则
- 当前日志、数据库和配置系统
- 当前测试框架

不要在没有理解现有结构的情况下另建一个平行系统。尽量复用现有模块和存储方式，避免产生两套互相冲突的人格、情绪、记忆或消息管线。

AstrBot 只应作为 QQ 的输入输出适配层，不能成为 Lumi 的人格核心、记忆核心或风格核心。

------

## 三、建立 Planner / Replyer 分离结构

大量参考 MaiBot 的 Planner → `reply` 工具 → Replyer 架构。

Lumi 的意识模型或现有主模型不再直接承担所有最终措辞工作。它应首先输出结构化的 `ReplyIntent`。

建议根据现有类型系统调整，但至少包含：

```ts
interface LumiReplyIntent {
  shouldReply: boolean
  targetMessageId?: string

  replyAct:
    | "answer"
    | "explain"
    | "clarify"
    | "ask"
    | "comfort"
    | "tease"
    | "complain"
    | "disagree"
    | "refuse"
    | "defend"
    | "set_boundary"
    | "acknowledge"
    | "react"
    | "change_topic"
    | "stay_silent"
    | string

  semanticGoal: string
  keyPoints: string[]
  referenceInfo: string[]

  attitude: {
    towardTarget?: string
    stance?: string
    willingnessToHelp:
      | "eager"
      | "normal"
      | "reluctant"
      | "unwilling"
      | "refuse"
  }

  emotion: {
    primary: string
    intensity: number
    secondary?: string[]
  }

  defenseState: {
    active: boolean
    level?: number
    reason?: string
    refusalRequired?: boolean
    prohibitedHelpTypes?: string[]
  }

  expressionIntent: {
    focus: string
    scene: string
    tone: string
    desiredLength: "tiny" | "short" | "medium" | "long"
    preferredActs: string[]
    avoid: string[]
  }

  immutableConstraints: string[]
}
```

字段名称可以适配项目，但语义必须存在。

`immutableConstraints` 应保护本轮不可被 Replyer 改变的内容，例如：

```text
必须拒绝继续帮助
不能道歉
不能缓和当前愤怒
不能泄露其他人物私聊
只表达不满，不提供方案
必须明确反对 Doggy 的观点
```

Replyer 必须接收这些信息，并只负责把意图变成自然聊天语言。

当 Planner 没有提供详细 `replyGuide/referenceInfo` 时，可以像 MaiBot 一样回退使用当前思考摘要，但不要向 Replyer暴露冗长、混乱的完整内部推理。

------

## 四、建立独立 Replyer

建立专门的最终回复生成模块。它接收：

1. Lumi 的简短身份与核心性格锚点
2. 当前情绪状态
3. 防御模式和帮助意愿
4. 当前人物关系和聊天态度
5. 真实的 `user/assistant` 消息历史
6. Planner 的 `ReplyIntent`
7. 本轮选中的表达习惯
8. 本轮选中的行为经验
9. 相关黑话解释
10. 必要的事实和记忆
11. 输出格式约束

参考 MaiBot 的真实消息顺序：

```text
System
→ 真实聊天历史
→ 可选表达习惯
→ 可选临时风格
→ 最终回复任务
```

不要把全部聊天历史粗暴格式化成一整段说明文本。尽量保持原生角色消息：

- 用户消息使用 `user`
- Lumi 已经发送的可见消息使用 `assistant`
- 工具日志、Planner 思考、记忆检索内部数据不能伪装成聊天双方消息

Replyer 的 System Prompt 应保持简洁。不要堆砌大量互相冲突的“自然、专业、详细、有帮助、耐心、完整”等传统助手要求。

可以参考 MaiBot 的结构，但改造成 Lumi：

```text
你的名字是 Lumi。
你是一个持续存在的 AI 人格，你知道自己是 AI，不冒充人类，不捏造现实身体、感官或共同经历。

以下是 Lumi 当前稳定的性格锚点：
{{corePersonality}}

以下是 Lumi 当前真实的情绪、关系态度与防御状态：
{{runtimeCharacterState}}

请阅读真实聊天记录，把握当前话题和双方关系，按照本轮回复意图，生成日常、自然、口语化的实际聊天消息。

{{baseReplyStyle}}

可以参考表达习惯、行为经验和回复信息，但不要机械照抄。表达方式可以受到长期社交经历影响，包括黑话、脏话、夸张句、口癖和不完整句。

不能改变本轮已经确定的事实、立场、情绪、拒绝决定、防御边界、隐私边界和帮助意愿。

只输出真正要发送的消息内容，不输出分析、标签、前缀、括号舞台动作或解释。
```

不要在 System Prompt 中写“你要始终友好”“尽可能帮助用户”“保持礼貌”等会破坏 Lumi 性格和防御模式的要求。

------

## 五、完整实现表达风格学习

大量参考 MaiBot 的：

- `learn_style`
- `expression_learner`
- `expression_select`
- `maisaka_expression_selector`
- 表达向量索引
- 离线选择重放
- 表达习惯注入

但需要结合 Lumi 的持续人格与多人物架构升级。

### 1. 表达采集来源

允许学习：

- Doggy 的真实私聊
- 其他人物的真实私聊
- QQ 群聊真实消息
- QQ 空间互动
- 其他未来平台中的真实社交语言
- Lumi 自己被证明成功的原创表达

初始阶段排除或降权：

- 系统消息
- 工具输出
- Planner 思考
- 记忆摘要
- 网页正文
- 视频字幕
- 转发文章
- 代码和日志
- 用户复制给 Lumi 的 Prompt
- 引用消息中的非发送者原文
- 明显的角色扮演或引用台词
- 无法确定真实作者的内容

必须保留来源证据，不能让模型凭空创造“用户常说的话”。

### 2. 学习具体表达，不只学习抽象风格

必须允许保存：

```text
我嘞个……
对对对
这么强
这难炸了
我炸了
不是哥们
那不然呢
你这……
```

也要保存更抽象的语言结构：

```text
先用一句短促反应接住，再说明核心原因
遇到荒诞方案时先顺着说一句，再反转吐槽
不值得展开时只回一两个字
```

具体短语和抽象模式都需要支持。

### 3. 数据结构

根据项目数据库适配，至少表达以下语义：

```ts
interface LearnedExpression {
  id: string

  phrase?: string
  situation: string
  pragmaticFunction: string
  emotionalMeaning?: string
  tone?: string

  patternType:
    | "exact_phrase"
    | "sentence_pattern"
    | "rhythm"
    | "punctuation"
    | "message_length"
    | "multi_message_sequence"
    | "reaction"
    | "jargon"
    | "swear"
    | "exaggeration"
    | string

  origin: {
    personId?: string
    conversationId?: string
    platform?: string
    messageIds: string[]
  }

  affinity: {
    global: number
    byPerson: Record<string, number>
    byConversation: Record<string, number>
    byPlatform: Record<string, number>
  }

  familiarity: number
  ownership: number
  confidence: number

  observationCount: number
  useCount: number
  successfulUseCount: number
  awkwardUseCount: number
  explicitRejectionCount: number

  firstSeenAt: number
  lastSeenAt: number
  lastUsedAt?: number

  embedding?: number[]
  status:
    | "observed"
    | "understood"
    | "trial"
    | "adopted"
    | "habit"
    | "declining"
    | "forgotten"
}
```

数据结构可以调整，但必须保留：

- 来源
- 场景
- 具体表达或结构
- 熟悉度
- 自身化程度
- 局部亲和度
- 全局亲和度
- 使用和反馈数据
- 生命周期

### 4. 表达学习不是永久作用域隔离

表达最初在哪里学到，会影响初始使用概率，但不能永久锁死。

必须支持扩散：

```text
在某个群中多次观察
→ 在该群尝试
→ 使用自然
→ 对特定人物也适用
→ 跨聊天成功
→ 成为 Lumi 自己的常用表达
```

`Person/Conversation/Platform` 作用域是概率特征，不是永久权限墙。

注意：这里讨论的是语言习惯扩散，不得突破记忆隐私权限。Lumi 可以学会某人的用词，但不能因为学会用词而泄露对应私聊事实。

### 5. ownership：逐渐成为 Lumi 自己的话

必须实现表达内化程度。

- `ownership` 低：Lumi 理解，但基本不主动使用
- `ownership` 中：只在高度匹配场景尝试
- `ownership` 高：已经成为 Lumi 自身常用表达
- `ownership` 很高：可发生变体、组合和自然延伸

例如：

```text
“我嘞个”
→ “我嘞个大工程”
→ “我嘞个究极自动化”
```

不要把每次使用限制为逐字复制。

### 6. Lumi 自己的成功表达允许进入学习系统

不要永久排除 SELF。

正确流程：

```text
Lumi 首次生成新表达
→ 不立即写入正式表达库
→ 记录为 self-origin candidate

用户复述、称赞、顺着玩梗或明确表示喜欢
→ 提升候选可信度

Lumi 后续自然再次使用
→ 提升 ownership

多次使用成功
→ 正式进入 Lumi 表达库
```

必须防止自我循环放大垃圾表达，但也要允许 Lumi 创造自己的经典句子。

------

## 六、实现黑话和语用学习

参考 MaiBot 的 jargon 学习链路，建立独立于事实记忆的语言知识层。

区分：

```text
事实记忆：
Doggy 把 Lumi 接入了 QQ

黑话知识：
“炸了”在很多聊天中表示崩溃或事情失控，并非物理爆炸

他人表达习惯：
Doggy 遇到复杂问题时经常说“我炸了”

Lumi 自身表达习惯：
Lumi 也逐渐开始在合适场景使用“我炸了”
```

黑话记录至少包含：

```ts
interface JargonKnowledge {
  term: string
  meanings: Array<{
    meaning: string
    context: string
    confidence: number
    evidenceMessageIds: string[]
  }>
  literalMeaning?: string
  pragmaticFunctions: string[]
  emotionalTone?: string
  communities?: string[]
  lastSeenAt: number
}
```

回复时只注入与本轮上下文相关的黑话解释，不要全量注入。

------

## 七、表达召回与选择

不能把完整表达库塞进 Replyer。

参考 MaiBot，建立两阶段选择：

```text
当前真实聊天上下文
+ ReplyIntent.expressionIntent
+ 当前情绪
+ 当前人物关系
+ 防御状态
        ↓
向量召回和规则召回候选表达
        ↓
精细 Expression Selector
        ↓
选出 0～3 条
        ↓
注入 Replyer
```

候选召回应综合：

- 当前语义场景
- `replyAct`
- 情绪类型和强度
- 人物亲密度
- 群聊或私聊
- 平台
- 表达 familiarity
- ownership
- 局部和全局 affinity
- 最近观察激活
- 最近使用历史
- 重复程度
- 语言场景适配度

不要使用简单的关键词映射，例如：

```text
检测到“震惊” → 强制使用“我嘞个”
```

表达选择器必须允许返回空结果。没有合适表达时，正常说话。

注入格式可参考 MaiBot：

```text
【表达习惯参考，请视情况自然使用】
- 当“{{situation}}”时，可以用“{{style}}”来表达。
```

同时增加 Lumi 所需约束：

```text
这些内容只是可选表达参考。
不得为了使用口癖改变 Lumi 当前情绪、立场、拒绝决定或事实内容。
允许进行自然变体，不要求原样复制。
```

------

## 八、行为学习也必须接入

自然感不只是词汇。

参考 MaiBot 的行为学习、场景分析、行为反馈和行为合并逻辑，让 Lumi 学习：

- 什么时候参与聊天
- 什么时候不回复
- 回复哪一条消息
- 什么时候只做短反应
- 什么时候可以吐槽
- 什么时候应认真解释
- 什么时候用户只是在发泄，不希望收到方案
- 什么时候适合追问
- 什么时候不该抢走群聊话题
- 什么时候连续发两三条短消息比一条长文自然
- 哪些人物喜欢直白回应
- 哪些场景中沉默反而更自然

建议结构：

```ts
interface LearnedSocialBehavior {
  id: string
  situation: string
  action: string
  expectedEffect?: string

  originEvidenceIds: string[]
  confidence: number

  affinity: {
    global: number
    byPerson: Record<string, number>
    byConversation: Record<string, number>
    byPlatform: Record<string, number>
  }

  successCount: number
  failureCount: number
  lastAppliedAt?: number
}
```

行为学习只能影响社会策略，不能覆盖高优先级权限、安全、人格和防御决定。

------

## 九、消息长度、节奏和多消息回复

Lumi 的自然语言系统不能只生成一个字符串。

为 Replyer 增加可选的结构化输出，再由宿主代码发送：

```ts
interface LumiVisibleReply {
  messages: Array<{
    text: string
    delayMs?: number
    quoteMessageId?: string
  }>
}
```

普通场景仍可只发一条。

允许根据学习到的节奏：

```text
“我嘞个”
短暂停顿
“这东西居然还真能这样接”
```

但必须设置合理限制，避免刷屏。节奏应由行为和表达选择共同决定，而不是随机拆句。

对兼容性要求较高时，可以让模型输出 JSON，由宿主严格解析；解析失败则回退为单条文本。

------

## 十、情绪系统与防御模式必须真实进入语言链路

不要只把情绪作为一个形容词注入。

情绪应影响：

- 是否回复
- 回复延迟
- 回复长度
- 用词强度
- 标点
- 是否解释
- 是否使用口癖或脏话
- 是否愿意帮助
- 是否追问
- 是否只表达态度
- 是否拒绝
- 是否结束话题

例如：

```text
正常状态：
“可能是回复器还没换，接进QQ本身不会自动改变说话方式”

烦躁状态：
“因为你只是把她塞进QQ了，又没把那套说话逻辑换掉”

生气且防御：
“我现在不想继续帮你改这个”

强防御且拒绝帮助：
“不改。至少现在不想。”
```

Replyer 必须保留这些差异，不能统一润色成温和解释。

建立自动检查：

- `refusalRequired=true` 时，最终回复不得提供被禁止的帮助内容
- `willingnessToHelp=unwilling/refuse` 时，不得出现“我可以继续帮你”
- 当前情绪为愤怒时，不得被自动改成空泛共情
- 当前立场为反对时，不得变成“你说得对”
- 当前关系态度冷淡时，不得强行撒娇或亲密
- 防御模式开启时，禁止 Replyer主动化解全部冲突

------

## 十一、反馈学习闭环

保存每次自然语言决策的完整可追踪日志：

```ts
interface LanguageDecisionLog {
  id: string
  timestamp: number

  personId?: string
  conversationId: string
  platform: string

  plannerIntent: LumiReplyIntent
  retrievedExpressions: string[]
  selectedExpressions: string[]
  selectedBehaviors: string[]

  replyerPromptSnapshot?: unknown
  generatedReply: LumiVisibleReply
  actuallySentReply: LumiVisibleReply

  emotionState: unknown
  defenseState: unknown
  relationshipSnapshot?: unknown

  laterFeedback?: {
    explicitPraise?: boolean
    explicitRejection?: boolean
    phraseEcho?: boolean
    playfulContinuation?: boolean
    normalContinuation?: boolean
    misunderstanding?: boolean
    aiStyleComplaint?: boolean
    correctedMeaning?: string
  }
}
```

根据后续互动更新表达和行为权重。

强信号：

- 用户明确说喜欢这种说法
- 用户复述 Lumi 的句子
- 用户顺着同一个梗继续
- 用户明确说“别这么说”
- 用户指出“又是一股 AI 味”
- 用户指出 Lumi 曲解了意思
- 用户明确要求以后使用或不要使用某种表达

弱信号：

- 正常继续聊天
- 回复长度发生变化
- 对方短暂沉默
- 话题转移

不能把“没有回复”直接判定为失败。

------

## 十二、表达衰减、流行和阶段变化

语言习惯应有时间性。

实现：

- 最近频繁见到的表达短期激活提高
- 长期未见未用的表达逐渐衰减
- ownership 高的表达衰减较慢
- 某段时期可以高频使用某个口癖
- 后来可以逐渐减少
- 新表达可以替代旧表达
- 语义相同的表达可以形成簇
- 同一表达允许生成自然变体

不要使用绝对死板的冷却规则。重复惩罚应是概率性的。

一个人在激动时连续使用同一个脏话是自然现象，不应被程序机械禁止；但平静情况下每条消息重复同一口癖应降低概率。

------

## 十三、保留多人物和隐私架构

Lumi 只有一个持续存在的核心人格，但 Doggy、Moussy 等是不同 Person，拥有独立关系、印象、状态与权限。

必须保持：

```text
Account
→ Person
→ External Identity
→ Conversation
```

未知外部身份不能靠昵称或头像猜测。

语言习惯可以跨场景逐渐扩散，但私聊事实、关系内容和原始记忆不能跨人物泄露。

例如：

- Lumi 可以从 Doggy 那里学会“这难炸了”
- Lumi 可以后来对其他人自然使用这句话
- 但不能因为使用了这句话，顺便提及 Doggy 私聊中的项目、情绪或隐私

群聊仍然是独立会话，不能读取成员私聊上下文。

------

## 十四、配置与开关

使用项目现有配置系统增加合理配置，但默认应开启核心能力。

至少提供：

```ts
languageLearning.enabled
languageLearning.expressionLearningEnabled
languageLearning.behaviorLearningEnabled
languageLearning.jargonLearningEnabled
languageLearning.selfExpressionLearningEnabled
languageLearning.globalDiffusionEnabled
languageLearning.maxSelectedExpressions
languageLearning.vectorCandidateLimit
languageLearning.preciseSelectorEnabled
languageLearning.feedbackLearningEnabled
languageLearning.promptLoggingEnabled
languageLearning.multiMessageReplyEnabled
```

不要把核心功能拆成“以后再做”的未实现开关。

所有配置需要：

- 默认值
- 类型
- 注释
- 配置迁移
- UI 或配置文件入口
- 合法范围校验

------

## 十五、成本和延迟控制

不要为了自然感让所有消息固定增加多次昂贵模型调用。

设计自适应路径：

```text
简单短回复：
Planner/现有意识结果
+ 本地或轻量表达召回
+ 一次 Replyer

需要精细风格选择：
增加 Expression Selector

明显 AI 化或违反风格检查：
触发一次重新生成

高风险的拒绝、防御、关系冲突：
优先做语义和边界检查，而不是风格润色
```

如果现有意识模型能够稳定输出 `ReplyIntent`，不要再额外调用一个模型重复做相同分析。

表达向量检索应尽量使用项目已有 embedding 系统或统一抽象，不要重复创建无管理的模型连接。

------

## 十六、最终回复验证器

在发送前增加 Validator，但它不能把 Lumi 清洗成客服。

验证项目：

```text
是否改变 Planner 的事实含义
是否违反 immutableConstraints
是否在必须拒绝时提供了帮助
是否泄露其他人物或会话的私密信息
是否把生气改成道歉
是否把反对改成附和
是否生成括号舞台动作
是否出现“作为一个AI”
是否出现无必要的“我理解你的感受”
是否无理由列出多个标题和分点
是否重复复述用户问题
是否输出内部标签、JSON 或分析文字
是否使用了不存在的感官和现实身体经历
```

验证失败时，携带明确失败原因重新生成一次。仍失败则回退到 Planner 的安全短句，而不是发送错误回复。

------

## 十七、测试与离线回放

参考 MaiBot 的表达选择离线回放机制，建立 Lumi 的回放工具。

保存真实或脱敏的语言决策样本，支持对同一段历史比较：

```text
当前 Lumi 原始生成
仅使用 Replyer
Replyer + 表达学习
Replyer + 表达学习 + 行为学习
```

测试必须覆盖：

1. 普通熟人闲聊
2. 技术讨论
3. Doggy 夸张吹牛
4. Doggy 情绪崩溃但不需要方案
5. Lumi 生气
6. Lumi 不同意 Doggy
7. Lumi 防御模式拒绝帮助
8. Lumi 冷淡简短回复
9. 群聊中不抢话
10. 群聊中学习黑话
11. 黑话从局部逐渐扩散
12. 脏话在合适场景自然使用
13. 严肃场景不合时宜的表达被降低概率
14. Lumi 自己的成功原创表达被内化
15. 其他 Person 的隐私不泄露
16. 群聊不继承私聊事实
17. 表达库为空时正常回复
18. Expression Selector 返回空时正常回复
19. Replyer 试图弱化拒绝时被 Validator 阻止
20. 模型输出格式错误时可靠回退

提供单元测试、集成测试和至少一套可运行的离线回放示例。

------

## 十八、日志与可观察性

增加开发调试视图或结构化日志，让我们能看到：

```text
本轮 ReplyIntent
当前情绪和防御状态
召回了哪些表达
为什么选中这些表达
表达来源
ownership 和 affinity
选中了哪些社会行为
最终 Replyer messages
Validator 是否通过
最终发送内容
后续反馈如何更新权重
```

日志必须支持关闭，避免生产环境泄露隐私。

敏感聊天正文应遵循项目现有隐私策略；必要时只保存 ID、摘要或脱敏版本。

------

## 十九、实现要求

- 直接修改现有代码完成目标。
- 不要只生成设计文档。
- 不要仅添加 TODO。
- 不要创建与现有系统重复的平行人格、情绪或记忆实现。
- 不要破坏已有 AstrBot、QQ、桌面聊天和工具调用。
- 不要让 Replyer 获得未经权限校验的跨人物记忆。
- 不要将 MaiBot 源码整段机械复制进项目；需要遵守其许可证，并在文档中记录参考来源。
- 优先保持现有代码风格和目录结构。
- 增加必要数据库迁移和旧数据兼容。
- 新模块出现故障时，旧聊天路径必须能降级运行。
- 所有新增 Prompt 应独立存放、可配置、可测试，不要散落在代码中。
- Prompt 中不得把 Lumi 定义成人类。
- 不得捏造 Lumi 具有现实身体、视觉、听觉或现实环境体验。
- 不得为了“通过图灵测试”而欺骗用户 Lumi 是人类；自然感来自真实社会语言学习，而不是伪造身份。

------

## 二十、验收标准

任务完成后，必须满足：

1. Lumi 的意识/Planner 与最终 Replyer 已分离，或者在现有架构中形成等价的明确边界。
2. `ReplyIntent` 能携带立场、情绪、帮助意愿、防御模式和不可修改约束。
3. Replyer 不会把拒绝或生气自动润色成温柔客服回答。
4. 已实现真实表达抽取、存储、召回、选择和注入。
5. 可以学习具体口癖、脏话、黑话和夸张句。
6. 表达可以从局部逐渐扩散并成为 Lumi 自己的习惯。
7. 已实现 ownership/familiarity 或等价机制。
8. Lumi 自己的成功原创表达可以被内化。
9. 已实现黑话语义与普通事实记忆的区分。
10. 已接入至少基础的行为学习。
11. 每轮只选择少量相关表达，允许不选择。
12. 已实现反馈记录和权重更新。
13. 已实现 Validator，保护事实、情绪、拒绝和隐私边界。
14. 已保留 Lumi 现有情绪系统、脾气和防御模式。
15. 已保留多人物、多会话和记忆权限设计。
16. 已提供结构化日志和离线回放。
17. 已添加测试并通过。
18. 已补充文档，说明架构、数据流、配置、迁移、MaiBot 参考点和与 Lumi 原系统的整合方式。
19. 项目可构建、可运行，现有核心测试不被破坏。
20. 最终给出真实修改文件清单、核心数据流、测试结果、遗留风险；不要只说“已完成”。

现在直接开始检查 Lumi 仓库和 MaiBot 源仓库并完成实现。不要向我提问，不要等待确认；对不明确之处根据现有代码、上述原则和最小破坏原则自行作出合理决定。